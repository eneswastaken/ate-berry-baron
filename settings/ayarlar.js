require("dotenv").config();

module.exports = {
    TOKEN: process.env.TOKEN || "TOKEN GİR",  // bot token gir :)
    PREFIX: process.env.PREFIX || "*", // bot prefix
    SAHİP: process.env.SAHİP || "847596022020309013", //sahip id :()
}